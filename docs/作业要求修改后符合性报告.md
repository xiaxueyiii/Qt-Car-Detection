# 作业要求修改后符合性报告

检查日期：2026-06-18  
项目路径：`E:\Desktop\FriD\suanfa\CarDet`

## 总体结论

修改后项目仍保持 Qt + C++ 桌面应用程序形态，主程序为 `QApplication + QMainWindow + QTabWidget`，数据库使用 SQLite，训练、推理、量化通过 C++ `QProcess` 调用 Python。标注、数据集、训练指标入库、菜单工具栏、数据库表展示等课程验收项已明显增强。

预计功能得分：必做项约 `93/100`，可选项约 `7/10`，总计约 `100/110`。  
主要扣分风险：C++ 直接加载量化模型推理目前是可编译条件接口和 GUI 降级提示，未完成完整 YOLO ONNX Runtime C++ 前后处理；本机命令行构建还受 Qt6/MinGW 工具链头文件 static_assert 阻塞。

## 构建检查

- `qmake`：成功。使用 `C:\Qt\6.11.0\mingw_64\bin\qmake.exe ..\CarDet.pro` 已生成 Makefile。
- Python 语法检查：通过。命令：`.venv\Scripts\python.exe -m py_compile scripts\train.py scripts\quantize.py scripts\quantize_onnx.py scripts\infer_image.py scripts\export_onnx.py`。
- 完整 C++ 编译：未成功。`mingw32-make` 在 Qt 6.11.0 头文件 `QtCore/qcomparehelpers.h` 触发 static_assert，错误信息为 `Use *_NON_NOEXCEPT version of the macro, or make the helper function noexcept`。该错误出现在编译 `main.cpp`、`MainWindow.cpp` 阶段，尚未进入项目业务代码。

## 逐项对照

| 编号 | 要求 | 修改后状态 | 预计得分 | 代码证据 | 风险 |
|---|---|---|---:|---|---|
| 1 | 使用 Qt、C++ 桌面应用 | 符合 | 10/10 | `CarDet.pro` 使用 `QT += widgets sql`；`src/main.cpp` 创建 `QApplication`；`MainWindow` 继承 `QMainWindow`。 | 命令行构建受工具链影响。 |
| 2.1 | 数据集管理 | 符合 | 5/5 | `DatasetPage::createDataset()` 创建 YOLO 目录；`importImageFolder()` 导入图片；`refreshImageTable()` 展示图像与标注状态。 | 导入默认进入 `train/images`，没有自动划分 train/valid。 |
| 2.2 | 矩形框创建、删除、移动、编辑大小 | 符合 | 8/8 | `AnnotationCanvas::mouseReleaseEvent()` 创建；`deleteSelected()` 删除；`MovingBox`、`ResizingBox` 处理移动和四角缩放。 | 旋转视图下编辑已做坐标变换，但复杂场景仍建议演示常规 0 度标注。 |
| 2.3 | 类别新建、删除 | 基本符合 | 3.5/4 | `AnnotationPage::addCategory()`、`deleteCategory()`、`refreshCategoryList()`；保存 `data.yaml` 和 SQLite `categories`。 | 删除类别不会自动重编号已有标注框。 |
| 2.4 | 标注框类别修改 | 符合 | 3/3 | `AnnotationPage::applyClassToSelectedBox()` 调用 `AnnotationCanvas::updateSelectedClass()`；表格编辑也可改类别。 | 无明显风险。 |
| 2.5 | 可选撤销/重做 | 基本符合 | 2.5/3 | `AnnotationCanvas::undo()`、`redo()`、`pushHistory()`。 | 历史栈为轻量实现，不是 `QUndoStack`。 |
| 2.6 | 可选已标注数据集管理 | 基本符合 | 2/3 | 图片列表显示每张图标注数量和类别；数据库 `images/annotations` 保存标注。 | 没有批量筛选“未标注/已标注”。 |
| 3.1 | GUI 设置训练集和验证集 | 符合 | 5/5 | `TrainingPage` 增加 `m_trainEdit`、`m_valEdit`；`TrainingViewModel` 传 `--train`、`--val`。 | 要求用户选择 images 目录。 |
| 3.2 | GUI 设置训练超参数 | 符合 | 5/5 | epochs、batch、image size、learning rate、model、device、output dir 均在 `TrainingPage`。 | 高级参数未全部暴露。 |
| 3.3 | 实时显示训练日志 | 符合 | 5/5 | `TrainingViewModel` 用 `QProcess` 读取 stdout/stderr；`TrainingPage::appendLog()` 显示。 | 无明显风险。 |
| 3.4 | 显示训练后指标和模型文件 | 符合 | 5/5 | `scripts/train.py` 输出 `FINAL_RESULT`；`TrainingPage::showMetrics()` 显示指标表。 | 依赖 Ultralytics 生成 `results.csv`。 |
| 3.5 | 可选指标可视化 | 部分符合 | 2.5/4 | `VisualizationPage` 展示 `training_runs.metrics_json`；训练页展示指标表。 | 未绘制曲线图，只展示表格/路径。 |
| 3.6 | 训练结果入库 | 符合 | 5/5 | `DatabaseManager::insertTrainingRun()`、`finishTrainingRun()` 保存参数、路径、指标 JSON、时间。 | 无明显风险。 |
| 3.7 | C++ 调 Python 训练 | 符合 | 5/5 | `TrainingViewModel::startTraining()` 使用 `QProcess` 调 `scripts/train.py`。 | 无明显风险。 |
| 4.1 | 菜单、工具栏、状态栏、布局 | 符合 | 4/4 | `MainWindow::setupChrome()` 增加菜单、工具栏、状态栏；Tab 页面保持。 | 无明显风险。 |
| 4.2 | GUI 设置训练和推理参数 | 符合 | 4/4 | 训练页和推理页参数均来自控件。 | 无明显风险。 |
| 4.3 | 图像放大、缩小、平移、旋转 | 基本符合 | 3.5/4 | `AnnotationCanvas::zoomIn/zoomOut/fitToWindow/rotateLeft/rotateRight`；右键/中键平移。 | 推理结果 QLabel 仍是静态预览，交互主要在标注画布。 |
| 4.4 | 列表展示图像和标注信息 | 符合 | 3/3 | `DatasetPage` 图像表；`AnnotationPage` 图片列表和标注表。 | 无明显风险。 |
| 5.1 | PTQ 或 QAT 至少一种 | 符合 | 5/5 | `scripts/quantize.py`、`quantize_onnx.py` 支持 ONNX 动态 INT8 PTQ。 | 无明显风险。 |
| 5.2 | ONNX Runtime/TensorRT/PyTorch 量化途径 | 符合 | 5/5 | `quantize_onnx.py` 使用 `onnxruntime.quantization.quantize_dynamic`。 | 依赖 Python onnxruntime。 |
| 6.1 | 多图像格式 | 符合 | 3/3 | 推理和标注支持 jpg、jpeg、png、bmp、webp。 | 无明显风险。 |
| 6.2 | QFileDialog 选择图像 | 符合 | 2/2 | `InferencePage::chooseImage()` 使用 `QFileDialog::getOpenFileName`。 | 无明显风险。 |
| 6.3 | GUI 展示推理结果 | 符合 | 3/3 | 结果图 QLabel + 检测结果表。 | 无明显风险。 |
| 6.4 | Python 加载模型推理 | 符合 | 3/3 | `InferenceViewModel` 调 `scripts/infer_image.py`；脚本 `YOLO(str(args.weights))`。 | 依赖 ultralytics。 |
| 6.5 | C++ 加载量化模型推理 | 部分符合 | 1/2 | `QuantizedOnnxRunner` 条件编译接口；`InferencePage` 提供“ONNX 量化模型优先使用 C++ Runtime”入口和降级提示。 | 未完成 YOLO ORT C++ 前后处理，严格验收可能扣分。 |
| 6.6 | 推理结果入库 | 符合 | 2/2 | `insertInferenceRecord()`、`insertInferenceObject()`，并镜像到 `inference_results`。 | 无明显风险。 |

## 仍可能扣分的项目

- C++ ONNX Runtime 量化模型推理不是完整实现，只是条件编译接口、session 入口和 GUI 降级提示。
- 指标可视化以表格和数据库展示为主，没有绘制训练曲线。
- 类别删除不会自动重编号历史标注，演示时建议说明“为避免破坏已有标签，不自动重排 class id”。
- 当前命令行构建受 Qt6/MinGW 工具链问题阻塞；建议在 Qt Creator 中使用可正常编译的 Kit，或修复 Qt6.11/MinGW 头文件 static_assert 问题。

## 预计得分

按上一版报告的估算权重：

- 必做项：约 `93/100`。
- 可选项：约 `7/10`。
- 合计：约 `100/110`。

若老师严格要求“C++ 完整执行量化 ONNX YOLO 推理”，该项仍会扣分；其他主要必做项已经有真实代码入口和数据库保存逻辑。
